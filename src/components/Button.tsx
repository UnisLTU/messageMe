const Button = () => {
  return (
    <button
      type="submit"
      className="w-64 h-12 rounded-lg mt-8 bg-gray-700 flex items-center justify-center"
    >
      <h1 className="text-slate-200 font-bold text-lg text-center">Sign In</h1>
    </button>
  );
};

export default Button;
